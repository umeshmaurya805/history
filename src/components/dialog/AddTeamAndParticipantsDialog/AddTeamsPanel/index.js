import React from "react";
import useStyles from "./style";

const AddTeamsPanel = () => {
  const classes = useStyles();

  return <div className={classes.root}>AddTeamsPanel</div>;
};

export default AddTeamsPanel;
