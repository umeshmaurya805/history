import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  title: {
    fontFamily: "Roboto",
    fontSize: 28,
    fontWeight: 900,
    textAlign: "center",
    marginBottom: "2.5rem",
  },
  competitiveLayout: {
    marginTop: "2.5rem",
  },
}));

export default useStyles;
