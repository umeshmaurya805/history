import React from "react";
import useStyles from "./style";

const NonCompetitivePanel = () => {
  const classes = useStyles();

  return <div className={classes.root}>NonCompetitivePanel</div>;
};

export default NonCompetitivePanel;
