import React from "react";
import useStyles from "./style";

const CompetitivePanel = () => {
  const classes = useStyles();

  return <div className={classes.root}>CompetitivePanel</div>;
};

export default CompetitivePanel;
