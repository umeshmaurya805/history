import React from "react";
import useStyles from "./style";

const WinnerPanel = () => {
  const classes = useStyles();

  return <div className={classes.root}>WinnerPanel</div>;
};

export default WinnerPanel;
